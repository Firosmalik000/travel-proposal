═══════════════════════════════════════════════════════════
  QUICK COMMANDS - Finance Menu Setup
═══════════════════════════════════════════════════════════

📌 CHOOSE ONE METHOD TO RUN SEEDER:

1️⃣ EASIEST - Double Click File:
   → seed-finance-menu.cmd

2️⃣ POWERSHELL (Recommended):
   cd C:\laragon\www\super-apps-xboss
   Set-ExecutionPolicy -Scope Process -ExecutionPolicy Bypass
   .\seed-finance-menu.ps1

3️⃣ COMMAND LINE:
   cd C:\laragon\www\super-apps-xboss
   php artisan db:seed --class=MenuSeeder

4️⃣ FULL RESET (⚠️ DELETES ALL DATA):
   cd C:\laragon\www\super-apps-xboss
   php artisan migrate:fresh --seed

═══════════════════════════════════════════════════════════
  VERIFICATION COMMANDS
═══════════════════════════════════════════════════════════

✅ Check via Tinker:
   php artisan tinker
   \App\Models\Menu::where('menu_key', 'finance')->first();
   exit

✅ List All Menus:
   php artisan tinker
   \App\Models\Menu::all()->pluck('name', 'menu_key');
   exit

✅ Count Menus:
   php artisan tinker
   echo \App\Models\Menu::count();
   exit

═══════════════════════════════════════════════════════════
  TROUBLESHOOTING COMMANDS
═══════════════════════════════════════════════════════════

🔧 Clear All Cache:
   php artisan cache:clear
   php artisan config:clear
   php artisan route:clear
   php artisan view:clear

🔧 Rebuild Autoload:
   composer dump-autoload

🔧 Delete Existing Finance Menu:
   php artisan tinker
   \App\Models\Menu::where('menu_key', 'finance')->delete();
   exit

🔧 Truncate Menus Table:
   php artisan tinker
   \App\Models\Menu::truncate();
   exit

🔧 Check Database Connection:
   php artisan tinker
   DB::connection()->getPdo();
   exit

═══════════════════════════════════════════════════════════
  MANUAL SQL (Use in phpMyAdmin if needed)
═══════════════════════════════════════════════════════════

-- Check if Finance exists:
SELECT * FROM menus WHERE menu_key = 'finance';

-- Delete Finance menu:
DELETE FROM menus WHERE menu_key = 'finance';

-- Count all menus:
SELECT COUNT(*) FROM menus;

-- List all menus:
SELECT id, name, menu_key, path FROM menus ORDER BY `order`;

═══════════════════════════════════════════════════════════
  AFTER SEEDING - ACCESS THE PAGE
═══════════════════════════════════════════════════════════

1. Login to: http://localhost/super-apps-xboss/public
2. Look for "Finance" in sidebar
3. Click Finance → Cashflow
4. URL should be: /dashboard/finance/cashflow

═══════════════════════════════════════════════════════════
  FILES CREATED FOR YOU
═══════════════════════════════════════════════════════════

Scripts:
  ✅ seed-finance-menu.ps1       - PowerShell script
  ✅ seed-finance-menu.cmd       - Windows batch script
  ✅ insert-finance-menu.sql     - Manual SQL script

Guides:
  ✅ FINANCE-MENU-SETUP-GUIDE.md - Complete documentation
  ✅ QUICK-COMMANDS.txt          - This file

Application Files:
  ✅ Migration: database/migrations/2025_01_22_000006_create_cashflows_table.php
  ✅ Model: app/Models/Cashflow.php
  ✅ Controller: app/Http/Controllers/Finance/CashflowController.php
  ✅ Routes: routes/web.php (updated)
  ✅ Seeder: database/seeders/MenuSeeder.php (updated)
  ✅ Frontend: resources/js/pages/Dashboard/Finance/Cashflow/Index.tsx
  ✅ Table: resources/js/pages/Dashboard/Finance/Cashflow/components/CashflowTable.tsx
  ✅ UI Component: resources/js/components/ui/textarea.tsx

═══════════════════════════════════════════════════════════
  NEED HELP?
═══════════════════════════════════════════════════════════

Read full guide: FINANCE-MENU-SETUP-GUIDE.md

Common Issues:
  ❌ MySQL not running    → Start Laragon, click "Start All"
  ❌ Database not found   → Create in phpMyAdmin
  ❌ Duplicate error      → Delete existing menu first
  ❌ Class not found      → Run: composer dump-autoload
  ❌ Menu not in sidebar  → Clear cache, rebuild frontend

═══════════════════════════════════════════════════════════
